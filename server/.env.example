# Copy this file to ".env" and fill in your own local values.
# Do NOT commit your real .env file.
# Note: port 5434 is used (not the default 5432) to avoid clashing with other
# local Postgres containers. See README.md for the docker run command.
DATABASE_URL="postgresql://toktickit:toktickit@localhost:5434/toktickit?schema=public"
PORT=3001
